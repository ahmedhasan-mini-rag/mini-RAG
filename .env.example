FILE_ALLOWED_TYPES=['text/plain', 'application/pdf']
FILE_MAX_SIZE=10
FILE_CHUNK_SIZE=512000 # 512 KB

# First 3 values here must match their corresponding ones in docker/.env
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB="mini-rag"
POSTGRES_PORT=5432
POSTGRES_HOST="localhost"

# llms settings

GENERATION_MODEL_PROVIDER="cohere"
EMBEDDING_MODEL_PROVIDER="cohere"

OPENAI_API_KEY=
OPENAI_BASE_URL=
COHERE_API_KEY=
GOOGLE_API_KEY=

GENERATION_MODEL_ID=
EMBEDDING_MODEL_ID=

# note that some embedding model don't support specifying the embedding size
# in this case, comment out the 'EMBEDDING_SIZE' field below to avoid errors
# EMBEDDING_SIZE=

DEFAULT_MAX_OUTPUT_TOKENS=1200
DEFAULT_MAX_INPUT_CHARS=2200
DEFAULT_TEMPERATURE=0.5

# vector dbs settings

VECTORDB_PROVIDER=
VECTORDB_SIMILARITY_METRIC="cosine"
VECTORDB_INDEX_BUILDING_THRESHOLD=10000
VECTORDB_INDEX_TYPE="hnsw"